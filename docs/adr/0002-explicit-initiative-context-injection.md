# Explicit Initiative Context Injection

Convergence does not automatically inject **Initiative Context** into a Session just because the Session is linked to an **Initiative**. Initiative state may be shown in the UI and offered as a provider-visible input source, but it must be explicitly visible and selectable before it affects an agent; this favors user control and debuggability over hidden convenience.
