---
'convergence': patch
---

Add PR-aware workspace lifecycle controls. Workspaces now show cached pull request status, can be archived without deleting conversation history, can be unarchived, and can remove their git worktree from disk behind an explicit confirmation while keeping historical sessions available.
