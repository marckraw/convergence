---
'convergence': patch
---

The context meter no longer reads a turn's summed usage as the context after the turn ends, nor a Task subagent's usage as the session's while the subagent runs; the auto-drill stops compacting small contexts.
