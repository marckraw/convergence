---
'convergence': patch
---

Session details for remote sessions now show where the run actually lives: the repository and branch the daemon materialized, and the pull request it opened when auto-PR fires.
