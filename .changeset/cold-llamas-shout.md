---
'convergence': patch
---

Add a "Remote" toggle to the composer for new project sessions: when a remote execution host is configured and the selected provider has a daemon counterpart, the session can be started on the remote daemon instead of this machine. This completes the user-facing path for remote session execution.
