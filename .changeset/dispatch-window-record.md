---
'convergence': patch
---

The Loom says 'dispatched HH:MM · Linear not yet In Progress' from the dispatch record instead of 'needs dispatch' during the window; the horse card names the dispatched ticket instead of 'No active ticket'; the card's ticket line opens the issue detail.
