---
'convergence': patch
---

Respect Pi model image support metadata, add safe outbound image diagnostics, and include local attachment paths so Pi can inspect files through its read tool when native image transport is unreliable.
