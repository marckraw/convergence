---
'convergence': minor
---

Merge reviewed: the mastermind seat can merge a wave's reviewed PRs from the app — on CLEAN and a green verify only, serially, waiting for the changesets run between merges; a failed step stops and raises a card.
