---
'convergence': patch
---

Extract the Provider Execution Host seam (interface, local adapter, contract tests) and define the versioned wire protocol for future remote provider execution. Internal refactor with no behavior change.
