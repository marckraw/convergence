---
'convergence': patch
---

Remote session failure notes are now actionable: they include the HTTP status and a hint matched to the failure kind (auth problems point at the token in Settings, unreachable daemons point at Test connection, configuration gaps point at the Remote execution host section).
