---
'convergence': patch
---

Replace project conversation rows with compact provider-tinted cards that expand when selected. Keep existing project actions, inline rename, workspace grouping and terminal rows, while showing provider, model and host/type identities at a glance.
