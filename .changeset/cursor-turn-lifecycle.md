---
'convergence': minor
---

A Cursor conversation keeps its process across turns, stops cleanly with a real cancel instead of ending failed, queues what you type mid-turn like the other providers, gives a silent turn a ten-minute budget, and recovers from a stale resume token. Manual context compaction is temporarily unavailable for Cursor while the conversation keeps its process across turns.
