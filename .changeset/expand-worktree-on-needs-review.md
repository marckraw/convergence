---
'convergence': patch
---

Auto-expand the parent worktree group in the sidebar when clicking a session from the "Needs Review" or "Waiting on You" lists. The sidebar previously opened the conversation but left the worktree collapsed, hiding where the session lived in the project tree.
