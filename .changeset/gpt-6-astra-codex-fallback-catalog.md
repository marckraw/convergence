---
'convergence': patch
---

The Codex fallback catalog knows GPT-6 Astra, Codex's new default model. It now mirrors the model list Codex 0.153 actually reports, so the models offered when Codex cannot be asked match the ones it can run.
