---
'convergence': patch
---

A Cursor turn whose whole answer is one `Error:` line now ends as failed with the error as a note, so the seat reads failed and no return is sent with a crash as its report.
