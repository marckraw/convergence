---
'convergence': patch
---

Add Settings → Shortcuts so users can customize the Command Center keyboard shortcut. `Cmd+K` / `Ctrl+K` remains the default, bindings persist in app settings, and conflicting terminal or dock shortcuts are rejected with inline feedback.
