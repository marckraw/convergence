---
'convergence': minor
---

Loom catches up faster: the tracker is re-read right after a dispatch or a return and when you come back to the window, there is a Refresh control, and it slows down when nobody is looking — never more than one read every ten seconds.
