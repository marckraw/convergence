---
'convergence': patch
---

Show the originating project name inline on idle terminal rows in the sidebar, next to the terminal name in a smaller muted font, so terminals are distinguishable at a glance without hovering.
