---
'convergence': patch
---

Show provider logos in session cards, the composer, model pickers, and provider settings. Keep model names easy to scan and provider names accessible in both themes (MAR-3000).

Stack provider and laptop/server icons below each sidebar card menu, with tooltips naming the provider and execution host.

Show running sessions across projects in a Working sidebar section. Keep pinned sessions in Pinned, prioritize requests for attention, and show when parallel tasks are still running after a response finishes.

Apply the compact identity-rail card layout: readable model names, status icons with labels, and Infinity for Resident / Clipboard List for Errand with named tooltips beneath the execution host.

Add a subtle Whisper background tint for each supported provider, preserving the hue on hover and selection and keeping metadata readable in light and dark themes. Use a thin, low-contrast selection outline so it complements the tint.

Add a collapsible activity toolbar with search, metadata filters, visible result counts, removable selections, grouping, sorting, and section ordering. Explain empty results and hidden pins, remember the view, and hold ordering during card interaction.

Show linked PR numbers and review states below the model. Show trustworthy elapsed turn or background-task time beside the status, freeze completed durations, and leave unknown timing unspecified.
