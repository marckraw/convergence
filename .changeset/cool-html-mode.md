---
'convergence': minor
---

Add HTML mode for conversation sessions. Users can toggle HTML mode from the composer, keep the Markdown transcript clean, generate companion HTML through a secondary same-provider agent, store living and per-turn HTML files in app-owned session storage, and preview generated HTML from the transcript.
