---
'convergence': patch
---

Bottom status bar: tooltips on the aggregate "running / need you" chip, per-project chips, and the recent-activity badge now appear near-instantly (100ms) on hover instead of inheriting the app-wide 1500ms delay. Other tooltips keep the existing delay.
