---
'convergence': patch
---

Add OpenRouter credential settings so users can paste an API key in Convergence, store it in macOS Keychain, and have Pi/OpenRouter sessions receive it automatically through `OPENROUTER_API_KEY`.
