---
'convergence': patch
---

Collapse the project actions menu into a single view. Clicking the actions trigger now opens the manage list directly, with inline expandable run output and an always-visible "Add action" row. The separate bottom log drawer has been removed.
