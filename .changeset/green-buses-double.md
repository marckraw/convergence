---
'convergence': patch
---

Fix the app settings dialog so long settings lists scroll correctly, use the
shared dark scrollbar styling, and present settings in clearer grouped sections
with better control alignment.
