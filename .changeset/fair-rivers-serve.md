---
'convergence': patch
---

Fix agent completion notifications so real session finish and attention
transitions trigger the same toast, sound, and system notification flow as
the manual notification test action.
