---
'convergence': patch
---

Remote sessions survive a stream drop across the daemon's pruned history, including its named replay; a viewer that cannot reach the host no longer marks the run failed, keeps a pending approval on the card through the outage, and lets go of its retry at once when stopped.
