{
  "name": "@convergence/execution-host-client",
  "version": "0.0.0",
  "private": true,
  "description": "The daemon client core: how an app talks to an agents-daemon over the execution host wire protocol",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config vitest.pure.config.ts",
    "test:pure": "vitest run --config vitest.pure.config.ts"
  },
  "dependencies": {
    "@mrck-labs/execution-host-protocol": "^0.14.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
