/** An open block's members hang under its line, drawn as today (MAR-3391). */
export const WORK_BLOCK_MEMBER_CLASS = 'ml-3 border-l border-border/60 pl-3'

/**
 * The model's line under a block's facts (MAR-3395 R5): muted, one line,
 * aligned with the facts' text past the chevron and the icon.
 */
export const WORK_BLOCK_SENTENCE_CLASS =
  'mt-0.5 truncate pl-[3.25rem] pr-2 text-xs text-muted-foreground/80'
